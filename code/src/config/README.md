# Config Directory

Contains centralized configuration files and definitions that drive the application's data-driven UI.

## Key Files
* **`tools.ts`**: Where all sidebar tools are defined. Defines item types, default dimensions, styles, and usage constraints (e.g., `maxQuantity`).
* **`TOOL_DEFINITIONS_README.md`**: A technical guide on how to extend the toolset and create custom "generator" tools.
# Lib Directory

Pure utility functions and TypeScript definitions shared across the application.

## Key Files
* **`utils.ts`**: Contains the core `CanvasItem` interface and general-purpose helper functions.
* **`canvasBounds.ts`**: Mathematical utilities for calculating bounding boxes and item collisions.
* **`textStyleOptions.ts`**: Configuration for typography tools, including supported fonts and alignment scales.